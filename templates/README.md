# Templates

`CLAUDE.md` in this folder is a generic starter scaffold — not specific to
this repo's content. When starting a new Claude Code project, copy it into
the new project's root as `CLAUDE.md` and fill in the placeholders. It's
kept here, versioned alongside this repo, purely so there's always a
known place to grab the latest copy from.
